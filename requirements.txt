numpy>=1.24
# Neural detector training (neural_sweep.py) uses Apple MLX and requires Apple Silicon:
mlx>=0.20
# Fast exact discrete-input rate estimators (mac_numba.py); optional, JIT-compiled:
numba>=0.59
# The figure (classical_curves.tex -> .pdf) additionally needs a LaTeX install with pgfplots
# (e.g. TeX Live), invoked as: pdflatex classical_curves.tex
